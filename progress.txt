# Ralph Progress Log
Started: Fri Feb 27 01:10:56 EET 2026
---

## Codebase Patterns
- Pre-commit hook runs `gofmt` automatically; always run `gofmt -w` before staging
- All new type fields use `omitempty` for backward compatibility
- OffsetPaginationOptions (offset-based) is distinct from PaginationOptions (token-based for search)

---

## 2026-02-27 - US-001
- Added OffsetPaginationOptions struct (StartAt, MaxResults) for offset-based pagination
- Extended Priority type with Description, StatusColor, IsDefault fields (all omitempty)
- Extended IssueType type with HierarchyLevel, Scope fields (all omitempty)
- Files changed: internal/api/types.go
- **Learnings for future iterations:**
  - All new fields use `omitempty` — fully backward-compatible with existing tests
  - Pre-commit hook runs `gofmt` automatically; always run `gofmt -w` before staging
  - OffsetPaginationOptions is distinct from PaginationOptions (token-based for search)
---

## 2026-02-27 - US-002
- Added 6 new types to internal/api/types.go: Field, FieldSchema, StatusDetail, LabelPage, ProjectDetail, ProjectSearchResult
- Verified CommentPage already exists with correct shape (Comments, MaxResults, Total, StartAt)
- Files changed: internal/api/types.go
- **Learnings for future iterations:**
  - StatusDetail is standalone (not embedding Status) to avoid JSON tag ambiguity — important pattern for any type that shares field names with another
  - LabelPage.Values is []string (not []Label) because GET /label returns PageBeanString
  - ProjectSearchResult uses 'values' field (PageBeanProject shape), not 'projects'
---

## 2026-02-27 - US-003
- Created internal/output/time.go with RelativeTime(iso8601 string) string function
- Thresholds: just now / minutes / hours / yesterday / days / months / years with singular/plural
- Handles both RFC3339 and Jira's millisecond format (2006-01-02T15:04:05.000-0700)
- Parse failure returns raw input unchanged (graceful degradation)
- Created internal/output/time_test.go with 28 test cases via table-driven tests and relativeTimeSince helper
- Files changed: internal/output/time.go, internal/output/time_test.go
- **Learnings for future iterations:**
  - Tests use relativeTimeSince(duration) for deterministic assertions — avoids flaky time.Now() comparisons
  - Jira timestamps may use millisecond format not covered by RFC3339 — always try secondary parse
  - RelativeTime is for table output only; JSON output always uses raw ISO 8601
---

## 2026-02-27 - US-004
- Created ToPlaintext(doc json.RawMessage) string in internal/adf/plaintext.go
- Structure-aware rendering: paragraphs, headings, bullet/ordered lists (with nesting), code blocks (4-space indent), blockquotes (> prefix), horizontal rules (---), hard breaks
- Inline marks: code→backtick-wrapped, link→'text (url)', bold/italic/strike→stripped
- Graceful degradation: empty/nil→"", invalid JSON→raw string as fallback
- 26 table-driven tests in internal/adf/plaintext_test.go covering every node type, nesting, marks, edge cases
- Files changed: internal/adf/plaintext.go, internal/adf/plaintext_test.go (new)
- **Learnings for future iterations:**
  - ToPlaintext is distinct from ExtractText — ToPlaintext does structured rendering, ExtractText does raw concatenation for truncated previews
  - ADF list items contain block-level children (usually paragraphs) — don't render listPrefix directly on ListItem, pass it down to the first child paragraph
  - Nested list depth is tracked via the depth parameter to renderNode — each nesting level adds 2 spaces of indentation
  - The mustMarshal test helper uses the Node constructor functions from nodes.go — no need to write raw JSON in tests
---

## 2026-02-27 - US-005
- Added OutputDryRunWithContext method to internal/output/formatter.go
- OutputDryRun now delegates to OutputDryRunWithContext(nil, ...) — fully backward-compatible
- OutputDryRunWithContext accepts extras map[string]interface{} merged at JSON top level alongside dry_run, payload, validation
- Added 3 tests: extras in JSON output, nil extras (backward compat), table mode with extras (ignored)
- Files changed: internal/output/formatter.go, internal/output/output_test.go
- **Learnings for future iterations:**
  - OutputDryRun delegates to OutputDryRunWithContext — when adding variants of existing methods, delegation avoids code duplication
  - nil map range in Go is a no-op — safe to iterate over nil extras without a guard check
---

## 2026-02-27 - US-006
- Created internal/api/comments.go with 5 methods: ListComments, GetComment, AddComment, UpdateComment, DeleteComment
- Added HTTP 413 (RequestEntityTooLarge) case to mapHTTPError in internal/api/errors.go — maps to VALIDATION_ERROR with comment size suggestion
- Created internal/api/comments_test.go with 14 test cases covering success, pagination, 404, 403, 413 for all CRUD operations
- Files changed: internal/api/comments.go (new), internal/api/comments_test.go (new), internal/api/errors.go
- **Learnings for future iterations:**
  - Comment API paths follow the pattern `issue/{key}/comment` and `issue/{key}/comment/{id}` — consistent with Jira REST v3
  - ListComments uses `orderBy=-created` query parameter for newest-first ordering
  - AddComment expects HTTP 201 (not 200) — Client.Do already handles this
  - DeleteComment expects HTTP 204 — Client.Do skips body decode automatically
  - HTTP 413 maps to VALIDATION_ERROR (not a distinct error code) since it's a payload size validation failure
---

## 2026-02-27 - US-007
- Created internal/api/projects.go with 2 methods: ListProjects, GetProject
- ListProjects uses offset-based pagination (startAt, maxResults) to GET /project/search
- GetProject accepts both project keys and numeric IDs via GET /project/{keyOrId}
- GetProject intercepts 404 errors and enriches them with project key in context
- Created internal/api/projects_test.go with 5 tests: success, pagination, by-key, by-numeric-ID, 404 with context
- Files changed: internal/api/projects.go (new), internal/api/projects_test.go (new)
- **Learnings for future iterations:**
  - mapHTTPError creates generic NOT_FOUND without context — call-site methods must intercept and re-wrap with entity-specific context (key/ID)
  - Project search uses PageBeanProject shape with 'values' array and 'isLast' boolean (unlike CommentPage which uses 'comments')
  - GET /project/{keyOrId} accepts both string keys ("PROJ") and numeric IDs ("10001") — tests should cover both
  - Query parameter construction: use sep variable pattern (`sep := "?"` → `sep = "&"`) for clean optional param chaining
---

## 2026-02-27 - US-008
- Created internal/api/schema.go with 6 methods: ListFields, ListIssueTypes, ListIssueTypesForProject, ListStatuses, ListPriorities, ListLabels
- All plain-array endpoints (field, issuetype, status, priority) return bare JSON arrays — decoded directly into slices
- ListIssueTypesForProject uses query param `projectId={id}` (requires numeric ID, not key)
- ListLabels is the only paginated schema endpoint — uses OffsetPaginationOptions with sep variable pattern
- Created internal/api/schema_test.go with 7 tests covering success cases and pagination
- Files changed: internal/api/schema.go (new), internal/api/schema_test.go (new)
- **Learnings for future iterations:**
  - Schema endpoints don't return entity-specific 404s (they return empty arrays) — no error enrichment needed
  - ListIssueTypesForProject requires numeric project ID — callers must resolve key→ID via GetProject first
  - Plain-array Jira endpoints decode directly into `[]T` — no wrapper struct needed
---

## 2026-02-27 - US-009
- Verified GetMyself(ctx) (*User, error) exists and returns full User struct via GET /myself
- Verified SearchUsers(ctx, query, startAt, maxResults) ([]User, error) exists via GET /user/search
- Verified error wrapping: 401→AUTH_ERROR (mapHTTPError), 429→handled by retry transport (retryPolicy)
- All required test coverage already present from Phase 2: empty search results, null email in response, GetMyself auth failure (401)
- No code changes needed — Phase 2 implementation fully satisfies Phase 3-4 requirements
- Files changed: none (verification-only story)
- **Learnings for future iterations:**
  - Phase 2 user API methods are complete — no need to add new methods for Phase 3-4
  - The retry transport handles 429 transparently; no per-method 429 handling needed
  - ResolveUser helper also already exists with comprehensive tests (@me, direct ID, single/no/ambiguous match)
---

## 2026-02-27 - US-010
- Implemented `jira comment list <issue-key>` command in internal/cmd/comment/list.go
- Table output: comment ID, author display name, created date (relative via RelativeTime), body (first 3 lines via ToPlaintext, truncated with '...')
- JSON output: offset-paginated list envelope with total; timestamps are raw ISO 8601 (not relative)
- Empty comments: text prints "No comments on PROJ-123"; JSON returns empty data array with pagination
- Issue not found: CLIError(NOT_FOUND) via mapHTTPError
- Pager enabled when stdout is TTY and --json not active
- Key auto-uppercased via issue.ValidateIssueKeyOrID (to be moved to shared in US-014)
- 10 tests covering: success, JSON, empty (text+JSON), 404, invalid key, pagination, ADF→plaintext, key auto-uppercase, truncateBody unit tests
- Files changed: internal/cmd/comment/list.go (new), internal/cmd/comment/list_test.go (new)
- **Learnings for future iterations:**
  - FetchOffsetPage wraps CommentPage into OffsetPageResult — need to map page.Comments to Items, page.StartAt/Total to the result struct
  - Comment commands import issue.ValidateIssueKeyOrID currently — US-014 will move to shared to avoid lateral dependency
  - truncateBody uses SplitN(text, "\n", maxLines+1) for efficient truncation without scanning the whole string
  - Pager lifecycle: StartPager() before table render, defer StopPager() — same pattern as issue list
  - Empty comments check: no CheckEmptyResultsAuth needed for comments (Jira properly returns 404 for invalid issues, unlike search which returns 200 with empty)
---

## 2026-02-27 - US-011
- Implemented `jira comment add <issue-key>` command in internal/cmd/comment/add.go
- Flags: --body (Markdown text), --body-file (file path or "-" for stdin), plus global --dry-run, --json, --quiet
- Body text converted Markdown → ADF via adf.Convert() before POST to API
- --body-file uses shared.ReadBodyFile (TTY rejection, 10MB cap, nonexistent file error)
- Validation: --body or --body-file required; neither → VALIDATION_ERROR with suggestion
- Success text: 'Added comment {id} to PROJ-123: https://{instance}/browse/PROJ-123?focusedCommentId={id}'
- --json: {"ok":true, "key":"PROJ-123", "comment_id":"10042", "action":"added", "url":"..."}
- --dry-run: validates issue exists via ListComments(maxResults=1), shows preview; JSON uses OutputDryRunWithContext with key
- --quiet: suppresses all output
- 11 tests: success, JSON shape, missing body, 404, 413, body-file, body-file-nonexistent, dry-run (text+JSON), quiet, invalid key
- Files changed: internal/cmd/comment/add.go (new), internal/cmd/comment/add_test.go (new)
- **Learnings for future iterations:**
  - Comment add follows exact same body resolution pattern as issue create: --body-file overrides --body, shared.ReadBodyFile handles stdin/file/TTY
  - Dry-run validation for comment commands uses ListComments with maxResults=1 — lightweight check that the issue exists without creating anything
  - Permalink URL format: `https://{instance}/browse/{key}?focusedCommentId={id}` — constructed client-side from client.Instance()
  - The 413 HTTP status maps to VALIDATION_ERROR via mapHTTPError (added in US-006) — no special handling needed in the command
---

## 2026-02-27 - US-012
- Implemented `jira comment edit <issue-key> <comment-id>` command in internal/cmd/comment/edit.go
- Two positional args: issue key (validated) + comment ID
- Flags: --body (Markdown text), --body-file (file path or "-" for stdin), plus global --dry-run, --json, --quiet
- Body text converted Markdown → ADF via adf.Convert() before PUT to API (same as comment add)
- Calls UpdateComment (PUT, expects HTTP 200) — IS idempotent (safe to retry)
- Success text: 'Updated comment {id} on PROJ-123'
- --json: {"ok":true, "key":"PROJ-123", "comment_id":"10042", "action":"updated"}
- --dry-run: validates comment exists via GetComment (proves both issue and comment exist), shows preview with OutputDryRunWithContext
- --quiet: suppresses all output
- 9 tests: success, JSON shape, missing body, comment 404, issue 404, body-file, dry-run (text+JSON), invalid key
- Files changed: internal/cmd/comment/edit.go (new), internal/cmd/comment/edit_test.go (new)
- **Learnings for future iterations:**
  - Comment edit is structurally identical to comment add — body resolution, ADF conversion, dry-run are all the same pattern
  - Dry-run for edit uses GetComment (validates specific comment + issue) vs add's ListComments (validates issue only)
  - Two positional args: use cobra.ExactArgs(2), access via args[0] and args[1]
  - Edit has no URL in success output or JSON (unlike add) — there's no "focused comment" action for edits
---

## 2026-02-27 - US-013
- Implemented `jira comment delete <issue-key> <comment-id>` command in internal/cmd/comment/delete.go
- Flags: --yes/-y (required for deletion), plus global --dry-run, --json, --quiet
- Requires --yes to confirm; without it: VALIDATION_ERROR with suggestion. --dry-run bypasses --yes requirement (non-destructive)
- Calls DeleteComment (DELETE, expects HTTP 204) — NOT idempotent (deleting already-deleted returns 404)
- Success text: 'Deleted comment {id} from PROJ-123'
- --json: {"ok":true, "key":"PROJ-123", "comment_id":"10042", "action":"deleted"}
- --dry-run: validates comment exists via GetComment, shows preview (ID, author, created, first line of body via ToPlaintext)
- --quiet: suppresses all output
- 8 tests: success, JSON shape, missing --yes, comment 404, issue 404, dry-run (text+JSON), invalid key
- Files changed: internal/cmd/comment/delete.go (new), internal/cmd/comment/delete_test.go (new)
- **Learnings for future iterations:**
  - Delete is the only comment command requiring --yes confirmation — destructive commands use BoolVarP with short flag "y"
  - --dry-run bypasses --yes: check DryRun before Yes in control flow to avoid false VALIDATION_ERROR on dry-run
  - Dry-run for delete fetches full comment via GetComment to show preview (author, created, body) — richer than edit's dry-run which only validates existence
  - Body preview in dry-run uses ToPlaintext + truncateBody(text, 1) for single-line preview
---

## 2026-02-27 - US-014
- Moved ValidateIssueKeyOrID from internal/cmd/issue/validate.go to internal/cmd/shared/validate.go
- Issue package validate.go now delegates to shared.ValidateIssueKeyOrID (backward-compatible wrapper)
- Issue validate_test.go simplified to smoke tests (comprehensive tests now in shared package)
- Updated all 4 comment commands (list, add, edit, delete) to import shared instead of issue
- Created internal/cmd/comment/comment.go with parent command registering all 4 subcommands
- Registered comment group on root command in internal/cmd/root/root.go
- Files changed: internal/cmd/shared/validate.go (new), internal/cmd/shared/validate_test.go (new), internal/cmd/issue/validate.go, internal/cmd/issue/validate_test.go, internal/cmd/comment/comment.go (new), internal/cmd/comment/list.go, internal/cmd/comment/add.go, internal/cmd/comment/edit.go, internal/cmd/comment/delete.go, internal/cmd/root/root.go
- **Learnings for future iterations:**
  - Moving shared utilities: keep a thin delegation wrapper in the original package to avoid touching every in-package caller
  - Comment parent command follows exact same pattern as issue parent: NewCmdXxx, RunE→cmd.Help(), AddCommand for each subcommand
  - Root command subcommand registration order: auth, issue, comment, search, config, alias (alphabetical within groups)
  - Pre-commit hook runs full test suite — no need to run tests manually before commit if you trust the hook
---

## 2026-02-27 - US-015
- Implemented `jira project list` command in internal/cmd/project/list.go
- Table output: KEY, NAME, LEAD (display name), TYPE (projectTypeKey)
- JSON output: offset-paginated list envelope with total
- Empty results: text prints "No projects found"; JSON returns empty data array with pagination
- Auth check: calls shared.CheckEmptyResultsAuth on empty results (Jira returns 200 with empty for unauthenticated)
- Pager enabled when stdout is TTY and --json not active
- 6 tests: success, pagination, empty (text+JSON), empty with auth check (401), JSON output shape
- Files changed: internal/cmd/project/list.go (new), internal/cmd/project/list_test.go (new), prd.json
- **Learnings for future iterations:**
  - Project list follows the exact same FetchOffsetPage pattern as comment list — map page.Values to Items (not page.Comments)
  - CheckEmptyResultsAuth is needed for any list endpoint that returns 200 with empty results for unauthenticated (project search, issue search) but NOT for endpoints that properly return 404 (comment list)
  - No validation on positional args needed for project list (no issue key) — simplest list command so far
---

## 2026-02-27 - US-016
- Implemented `jira project view <key-or-id>` command in internal/cmd/project/view.go
- Table output: key, name, lead (display name), description (truncated to 5 lines), type (projectTypeKey), issue types (comma-separated), URL
- JSON output: bare object (no envelope) via formatter.RawJSON — same pattern as issue view
- Project not found: CLIError(NOT_FOUND) with suggestion, exit 4 — handled by GetProject's error enrichment
- --quiet: suppresses all output
- 4 tests: success, 404, JSON shape, issue types comma display
- Files changed: internal/cmd/project/view.go (new), internal/cmd/project/view_test.go (new), prd.json
- **Learnings for future iterations:**
  - Single-entity view commands use formatter.RawJSON for JSON mode (bare object) vs list commands which use formatter.OutputList (envelope with pagination)
  - Project description is plain string (NOT ADF) per Swagger spec — no adf.ToPlaintext() needed, just truncate by lines
  - GetProject already enriches 404 errors with project key context — the command doesn't need to do its own error wrapping
  - Project view has no --fields flag (simpler than issue view) and no --no-pager (single entity, not paginated)
---

## 2026-02-27 - US-017
- Created internal/cmd/project/project.go with parent command registering list and view subcommands
- Registered project group on root command in internal/cmd/root/root.go
- `jira project --help` shows list and view subcommands
- Files changed: internal/cmd/project/project.go (new), internal/cmd/root/root.go
- **Learnings for future iterations:**
  - Project parent command follows exact same pattern as comment parent: NewCmdXxx, RunE→cmd.Help(), AddCommand for each subcommand
  - Root command registration order: auth, issue, search, comment, project, config, alias
  - Pre-commit hook runs full test suite including build — if hook passes, commit is safe
---

## 2026-02-27 - US-018
- Implemented `jira user search <query>` command in internal/cmd/user/search.go
- Table output: account ID, display name, email (or '(hidden)' when null), active status (yes/no)
- JSON output: raw-array list envelope with null total; has_next_page inferred from len(results)==limit
- Empty results: text prints "No users matching 'xyz'"; JSON returns empty data array with null total
- No --offset flag (Jira user search startAt is unreliable per SPEC deviation)
- --limit defaults to 10
- 5 tests: success, empty results, null email handling, JSON output shape (null total + camelCase fields), has_next_page inference
- Files changed: internal/cmd/user/search.go (new), internal/cmd/user/search_test.go (new), prd.json
- **Learnings for future iterations:**
  - User search returns plain []User array — no pagination envelope from Jira, so PaginationMeta is constructed manually with Total: nil
  - has_next_page is inferred from len(results) == limit (if exact match, probably more exist)
  - No --offset flag by design — Jira's user search startAt is unreliable, always use startAt=0
  - Test handler path matching: use strings.Contains(path, "/user/search") not strings.HasPrefix — the API client prepends /rest/api/3/
---

## 2026-02-27 - US-019
- Implemented `jira user me` command in internal/cmd/user/me.go
- Table output: account ID, display name, email (or '(hidden)' when null)
- JSON output: bare object with Jira API camelCase field names (accountId, displayName, emailAddress)
- Auth failure propagated from f.APIClient() → GetMyself
- 3 tests: success, null email, JSON output shape with camelCase field verification
- Files changed: internal/cmd/user/me.go (new), internal/cmd/user/me_test.go (new), prd.json
- **Learnings for future iterations:**
  - User me is the simplest view command — no positional args, no pagination, no pager; bare object JSON via formatter.RawJSON
  - Null email handling pattern: `*string` field, check nil before display, show "(hidden)" for text mode
  - Test factory pattern reused from search: newTestUserMeFactory creates httptest server + pre-wired factory
  - myselfHandler matches on strings.HasSuffix(path, "/myself") — consistent with API client path construction
---

## 2026-02-27 - US-020
- Created internal/cmd/user/user.go with parent command registering search and me subcommands
- Registered user group on root command in internal/cmd/root/root.go
- `jira user --help` shows search and me subcommands
- Files changed: internal/cmd/user/user.go (new), internal/cmd/root/root.go
- **Learnings for future iterations:**
  - User parent command follows exact same pattern as comment and project parents: NewCmdXxx, RunE→cmd.Help(), AddCommand for each subcommand
  - Root command registration order: auth, issue, search, comment, project, user, config, alias
---

## 2026-02-27 - US-021
- Implemented `jira schema fields` command in internal/cmd/schema/fields.go
- Table output: field ID, name, schema type, custom flag (yes/no)
- JSON output: unpaginated list envelope (pagination: null)
- Forward-compat flags: --project and --type accepted but no-op with stderr warning
- 5 tests: success, JSON shape (null pagination + field structure), --project warning, --type warning, data identical with and without flags
- Files changed: internal/cmd/schema/fields.go (new), internal/cmd/schema/fields_test.go (new), prd.json
- **Learnings for future iterations:**
  - Schema commands use unpaginated list envelope: `formatter.OutputList(items, nil, tableFunc)` — nil pagination produces `"pagination": null` in JSON
  - Forward-compat no-op flags pattern: accept the flag, emit `fmt.Fprintln(f.IOStreams.Err, "Warning: ...")`, proceed with full data — lets agents use flags now without breaking when filtering is added later
  - Schema command test factory pattern: same as user search — `newTestSchemaFieldsFactory` creates httptest server + pre-wired factory
  - Handler matches on `strings.HasSuffix(r.URL.Path, "/field")` — consistent with API client path construction
---

## 2026-02-27 - US-022
- Implemented `jira schema types` command in internal/cmd/schema/types.go
- Without --project: calls ListIssueTypes (GET /issuetype, all types globally)
- With --project: resolves project key to numeric ID via GetProject, then calls ListIssueTypesForProject
- Table output: name, description (truncated to 60 chars), subtask flag (yes/no), icon URL
- JSON output: unpaginated list envelope (pagination: null) with full IssueType fields (id, name, description, subtask, iconUrl, hierarchyLevel, scope)
- Project not found: CLIError(NOT_FOUND) via GetProject error enrichment
- 4 tests: global types, project-scoped types, project 404, JSON output shape
- Files changed: internal/cmd/schema/types.go (new), internal/cmd/schema/types_test.go (new), prd.json
- **Learnings for future iterations:**
  - Schema types is the first schema command with functional --project flag (not no-op) — resolves key→ID via GetProject then calls ListIssueTypesForProject
  - Both branches return []api.IssueType — use concrete typed variable, not interface{}, for clean table rendering
  - Error message "NOT_FOUND: No project could be found" — when checking lowercased error strings, match "not_found" (underscore) not "not found" (space)
  - truncateDescription is a simple char-level truncation; distinct from truncateBody (line-level in comment list)
---
